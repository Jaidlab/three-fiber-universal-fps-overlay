{
  "name": "three-fiber-universal-fps-overlay",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "funding": "https://github.com/sponsors/Jaid",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jaidlab/three-fiber-universal-fps-overlay.git"
  },
  "imports": {
    "#src/*": "./src/*"
  },
  "scripts": {
    "build": "bun run ./script/build.ts",
    "check": "bun run lint && bun run typecheck && bun run test && bun run build",
    "lint": "eslint .",
    "test": "bun test --max-concurrency 1 ./test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "bun-types": "^1.3.14",
    "eslint": "^10.8.1",
    "eslint-config-jaid": "^26.0.0",
    "tsconfig-jaid-bun": "^15.2.0",
    "typescript": "~6.0.0"
  }
}
